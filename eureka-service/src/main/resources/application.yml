server:
  port: 8761

spring:
  application:
    name: eureka-service

# 유레카 서버 자신은 레지스트리에 등록/조회하지 않음
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
